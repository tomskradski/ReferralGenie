package com.radar.JPA.Repositories;

import com.radar.JPA.Entities.ReferredPracticeEntity;
import org.springframework.data.repository.CrudRepository;

public interface ReferredPracticeRepository extends CrudRepository<ReferredPracticeEntity, Long> {
}
